Using "RC_Core.rcm"
; Spawn-init: names the Test Zone Rat Catcher quest-giver.
; NPCs default to their Race ("Human") without this. SetName is privileged -> allowlisted.

Function Main()

	NPC = Actor()
	SetName(NPC, "Rat Catcher")

	Return

End Function
