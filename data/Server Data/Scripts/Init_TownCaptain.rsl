Using "RC_Core.rcm"
; Spawn-init: names the Plains quest-giver Captain ("Raiders at the Gate").
; NPCs default to their Race ("Human") without this. SetName is privileged -> allowlisted.

Function Main()

	NPC = Actor()
	SetName(NPC, "Town Captain")

	Return

End Function
