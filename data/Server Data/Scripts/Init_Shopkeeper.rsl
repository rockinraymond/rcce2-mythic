Using "RC_Core.rcm"
; Spawn-init: names the Plains General Store merchant.
; NPCs default to their Race ("Human") without this. SetName is privileged -> allowlisted.

Function Main()

	NPC = Actor()
	SetName(NPC, "Shopkeeper")

	Return

End Function
