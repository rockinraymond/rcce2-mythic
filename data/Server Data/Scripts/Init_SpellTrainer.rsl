Using "RC_Core.rcm"
; Spawn-init: names the Plains spell trainer (replaces the leftover Spawn_Test init that named it "Test Human").
; NPCs default to their Race ("Human") without this. SetName is privileged -> allowlisted.

Function Main()

	NPC = Actor()
	SetName(NPC, "Spell Trainer")

	Return

End Function
